export const environment = {
  production: true,
  /**
   * Flipped to false in milestone 2, once the Spring Boot API exists. It is the
   * only line that changes to move the whole app from mock data to the real
   * backend — see the API token bindings in app.config.ts.
   */
  useMocks: true,
};
